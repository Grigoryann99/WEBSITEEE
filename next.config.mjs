const nextConfig = {
    images: {
        domains: [
            "images.unsplash.com",
            "source.unsplash.com",
            "upload.wikimedia.org",
            "images.pexels.com"
        ]
    }
};

export default nextConfig;
